mkdir -p ~/(workspace name)/src

cd ~/(workspace name)/src

catkin_create_pkg spawn rospy geometry_msgs turtlesim std_srus

cd..

catkin_make

source devel/setup.bash

rosrun turtlesim turtlesim_node [new terminal]

rosservice call /spawn 2 2 0 turtle2 [new terminal]

[operate turtle 1 with teleoperate]
rosrun turtlesim turtle_teleop_key [new terminal]

cd ~/(workspace name)/src/spawn/scripts

gedit follower.py

chmod +x follower.py

roscore [new terminal]

[new terminal]
cd ~/(workspace name)
source devel/setup.bash
rosrun spawn follower.py

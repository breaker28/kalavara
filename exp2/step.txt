mkdir -p ~/(workspace name)/src

cd ~/(workspace name)

catkin_make

source devel/setup.bash

cd ~/(workspace name)/src

catkin_create_pkg mathematical_operator std_msgs rospy

cd ~/(workspace name)/src/mathematical_operator

mkdir scripts

gedit scripts/operator.py

chmod +x operator.py

cd ~/(workspace name)

catkin_make

source devel/setup.bash

roscore (new terminal)

cd ~/(workspace name) [new terminal]

source devel/setup.bash

rosrun mathematical_operator operator.py
